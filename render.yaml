services:
  - type: web
    name: summarizer-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 1 --threads 1 --preload
    envVars:
      - key: MODEL_NAME
        value: sshleifer/distilbart-cnn-12-6
